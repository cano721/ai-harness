_schema_version: 1
teams:
  - backend
  - frontend
guardrails:
  max_files_changed: 25
  max_cost_usd: 10.0
  max_execution_minutes: 60
hooks:
  lighthouse:
    enabled: false
    reason: "SSR 프로젝트"
rules:
  test_coverage: 70
