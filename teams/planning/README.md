# Planning Team Assets

`teams/planning` contains the two concrete planner-mode bundles.

- `bundle-codex/` is the Codex shipping bundle.
- `bundle-claude/` is the Claude Code shipping bundle.

When you update planner-mode installation, change files under the runtime-specific bundle that is actually shipped.
