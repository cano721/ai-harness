interface:
  display_name: "Implement Feature"
  short_description: "Plan, approve, and deliver features by project policy."
  default_prompt: "Use $implement-feature to plan this feature with me, wait for approval, then deliver it using this project’s test policy: $ARGUMENTS"
